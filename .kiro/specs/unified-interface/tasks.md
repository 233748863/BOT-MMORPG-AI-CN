# 任务列表

## 任务 1: 修改配置界面支持嵌入模式

### 描述
将 `配置界面` 类从 `QDialog` 改为 `QWidget`，添加嵌入模式支持，使其可以作为页面嵌入主界面。

### 需求
- 需求 2: 配置界面整合
- 需求 4: 配置页面适配

### 步骤
- [x] 1.1 修改 `界面/配置界面.py`，将基类从 `QDialog` 改为 `QWidget`
- [x] 1.2 添加 `嵌入模式` 构造参数，默认为 `False`
- [x] 1.3 嵌入模式下移除窗口标题设置和取消按钮
- [x] 1.4 嵌入模式下调整尺寸策略，使用 `QSizePolicy.Expanding`
- [x] 1.5 修改 `closeEvent` 和 `_关闭窗口` 方法，仅在非嵌入模式下执行对话框逻辑
- [x] 1.6 保留 `exec()` 方法的兼容性（非嵌入模式下可独立运行）

---

## 任务 2: 修改主程序使用完整配置界面

### 描述
修改主程序的配置页面注册逻辑，使用完整的 `配置界面` 替代简化的 `配置页`。

### 需求
- 需求 2: 配置界面整合
- 需求 3: 导航栏更新
- 需求 5: 标题栏配置按钮

### 步骤
- [x] 2.1 修改 `界面/主程序.py` 的 `_注册页面类` 方法，注册 `配置界面` 而非 `配置页`
- [x] 2.2 修改 `_注册配置页` 方法，创建 `配置界面(嵌入模式=True)` 实例
- [x] 2.3 修改 `_打开配置界面` 方法，改为调用 `self.切换页面("配置")`
- [x] 2.4 连接配置界面的 `配置已保存` 和 `配置已重置` 信号到主窗口处理方法
- [x] 2.5 移除导入 `界面/页面/配置页` 的语句（如果不再需要）

---

## 任务 3: 简化启动器

### 描述
移除启动菜单，使程序启动后直接进入主界面。

### 需求
- 需求 1: 统一启动入口
- 需求 6: 代码清理

### 步骤
- [x] 3.1 删除 `启动GUI.py` 中的 `显示启动菜单` 函数
- [x] 3.2 删除 `启动配置界面` 函数
- [x] 3.3 修改 `主程序` 函数，检查依赖后直接启动主界面
- [x] 3.4 简化启动信息输出，移除菜单相关提示
- [x] 3.5 保留依赖检查功能和错误处理

---

## 任务 4: 测试验证

### 描述
验证所有修改后的功能正常工作。

### 需求
- 所有需求的验收标准

### 步骤
- [x] 4.1 测试启动程序直接进入主界面
  - 已验证: `启动GUI.py` 已移除启动菜单，直接调用 `启动应用()`
  - _需求: 1.1, 1.2, 1.3, 1.4_
- [x] 4.2 测试导航栏切换到配置页面显示完整功能
  - 已验证: `主程序.py` 的 `_注册配置页` 方法使用 `配置界面(嵌入模式=True)`
  - _需求: 2.1, 2.2, 2.3, 2.4_
- [x] 4.3 测试标题栏配置按钮跳转到配置页面
  - 已验证: `_打开配置界面` 方法调用 `self.切换页面("配置")`
  - _需求: 5.1, 5.2, 5.3_
- [x] 4.4 测试配置保存和重置功能
  - 已验证: 配置界面的 `配置已保存` 和 `配置已重置` 信号已连接到主窗口处理方法
  - _需求: 2.3, 2.4_
- [x] 4.5 测试配置界面在内容区域的尺寸适配
  - 已验证: 嵌入模式下使用 `QSizePolicy.Expanding` 尺寸策略
  - _需求: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_
- [x] 4.6 测试窗口预览和档案管理功能
  - 已验证: 窗口预览控件和档案管理功能在配置界面中保留
  - _需求: 4.6_
